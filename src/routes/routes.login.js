import { LoginLayout } from "../layouts"
import { Login } from "../pages"

const routesLogin = [
    {
        path:"/",
        layout: LoginLayout,
        component: Login,
    },
]

export default routesLogin;